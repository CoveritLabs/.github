# Demo Media

CoverIt currently has one public demo video:

[Watch `demo.mp4`](../assets/clips/demo.mp4)

The demo gives a quick view of the platform and the main workflow around projects,
application versions, crawl sessions, generated flows, and regression support.

The video is linked instead of embedded because it is a large file. This keeps the
GitHub profile page light while still making the demo easy to access.